version: '2'

services:
  webserver:
    image: node:16.15
    volumes:
      - .:/app
    command: >
      sh -c "yarn && yarn build && yarn serve"
    working_dir: /app
    ports:
      - 80:80
    environment:
      NVM_DIR: /root/.nvm
